<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/scroll_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:paddingBottom="@dimen/homeFragmentMarginBottom"
    tools:context=".ui.fragments.AccountFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/fragment_account_logged_in"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/accountLayoutMarginHorizontal"
            android:layout_marginTop="@dimen/accountLayoutMarginTop"
            android:layout_marginEnd="@dimen/accountLayoutMarginHorizontal">

            <include
                layout="@layout/fragment_account_logged_in"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>

        </LinearLayout>

        <LinearLayout
            android:id="@+id/fragment_account_not_logged_in"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/accountLayoutMarginHorizontal"
            android:layout_marginTop="@dimen/accountLayoutMarginTop"
            android:layout_marginEnd="@dimen/accountLayoutMarginHorizontal"
            android:visibility="gone">

            <include
                layout="@layout/fragment_account_not_logged_in"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>

        </LinearLayout>

    </LinearLayout>

</androidx.core.widget.NestedScrollView>
